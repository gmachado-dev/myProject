const somar = (numeroA, numeroB,) => numeroA + numeroB;
let operacao = "numeroA + numeroB";
const calculadora = (numeraA, numeroB, operacao) => operação(numeroA, numeroB);

console.log(calculadora(10, 20, (numeroA, numeroB) => numeroA + numeroB));

